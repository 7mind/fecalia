# CLAUDE.md

This project's agent instructions are maintained in a single, tool-agnostic file:

➡️ **See [AGENTS.md](AGENTS.md).**

It covers the build/test/lint commands, the load-bearing invariants you must not
break, the testing discipline, and — importantly — the rule to **keep the docs
(README.md, docs/design.md, docs/install.md) in sync with code changes in the
same change**.

Everything in AGENTS.md applies to Claude Code. This file is intentionally a
thin pointer so the two never diverge; edit AGENTS.md, not this file.
