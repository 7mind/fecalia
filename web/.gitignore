node_modules/

# Build output — vite.config.ts emits it outside this directory, at
# internal/monitor/dist (see internal/monitor/.gitignore); nothing builds
# to web/dist, but ignore it defensively.
/dist/
