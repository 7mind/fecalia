// Package metrics exposes a localhost-bound Prometheus /metrics endpoint with
// per-path counters and gauges (bytes, loss, RTT, jitter, throughput, path
// state, FEC overhead/recovery).
package metrics
