// Package config loads and validates the single TOML configuration shared by
// both roles (edge and concentrator): role, per-path source addresses, WireGuard
// keys/peers, amnezia obfuscation parameters, and the outer-control PSK.
package config
