# Vite build output (web/vite.config.ts outDir) — generated by `npm run
# build` (run from web/; wired into the Justfile's `web-build` step in
# T167) and consumed by a //go:embed added in T167. Build artifact, not
# source: never commit.
/dist/
