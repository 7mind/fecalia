// Package bind implements the custom conn.Bind that lives under the embedded
// amneziawg-go engine: per-path UDP sockets behind a single virtual endpoint,
// endpoint roaming, and MTU accounting.
package bind
