# Candidate planner (opus) — G13 round 1

- **goal:** G13 (milestone M50)
- **role:** candidate planner (opus-4.8[1m]), multi-planner CANDIDATE mode
- **outcome:** candidate plan emitted — 5 milestones, 6 tasks
- **agent-id:** ac5d6e2c7eba4f406

## Session summary (verbatim)

- **Role:** candidate planner (opus), goal G13 "Operability & safety for weighted aggregation + pacing", CANDIDATE mode (read-only; nothing written to any ledger).
- **Did:** Grounded read-only against the ledger (G13 + answered Q51–Q55) and the real repo via codegraph (internal/sched/weighted.go, internal/sched/scheduler.go, internal/config, internal/metrics, internal/device/metrics.go, internal/bind probe.go/multipath.go, internal/frame, internal/telemetry, test/e2e), then emitted one candidate task-DAG.
- **Achieved:** A 5-milestone / 6-task candidate honoring all binding scope decisions — item 1 observability (metric+log, per-peer, Q54), item 2 capacity-sanity guard (fail-fast declared / WARN+gauge undeclared, Q52), item 3(ii) PROBE-frame protection (Q51), the pacing-tradeoff + inner-ICMP-infeasibility docs, and an up-front shared e2e overload fixture (Q55) — while explicitly NOT re-owning G2/Q20's auto-derive/BDP docs (Q53). Counts: 5 milestones, 6 tasks.
- **Discovered (load-bearing):** PROBE frames never traverse the scheduler/pacer — they egress directly via emitProbes()→WriteToUDPAddrPort (internal/bind/probe.go:46-57), bypassing Send→Pick→token-bucket. So Q51's "protect probes from pacer shedding" cannot be a ClassControl-style exemption; it must shape DATA egress to reserve link headroom (encoded as a reproduce-first probe-egress reserve). Also: no scheduler→metrics seam today (metrics.Source = Paths/FEC/Reseq/Session/PeerNames), so item 1 needs a new snapshot accessor + Source.Aggregation() + collector gauges plumbed through Bind PeerSnapshots and the device metricsSource; deriveWeightedPacingFromBDP/SizePacingFromBDP already exist (G2 scope).
- **Issues:** The docs task cannot be e2e-gated (no runtime surface) — flagged this explicit deviation from Q55's e2e-only rule and gate it on just lint + reviewer prose-check instead; behavioral tasks still update their own operator docs in-change per AGENTS.md.
