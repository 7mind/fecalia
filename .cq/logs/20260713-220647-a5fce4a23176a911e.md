# plan-advance — goal G5 — DEFAULT single-planner mode (revision pass, tier frontier)

Round: /cq:plan:advance G5, revision applying reconciled review R70 (revise, 2 criticisms).
Returned status token: review-requested.

### Session summary
- **Did:** Rule 3 (revise with empty `new_questions`): applied R70's two [fable] criticism entries by revising tasks in place; no phase change, no new tasks/milestones.
- **Achieved:** `update_item` on three tasks —
  - **T74** (M21): specified the FIRST-RESOLVE INSTALL PATH — after an endpoint-less tolerant boot, the first successful resolve must install the resolved endpoint on the engine peer via the UAPI/IpcSet `endpoint=` path (SetPeerRemote only repoints bind remotes and never populates the engine peer's endpoint, so a bare rehandshake could not transmit), then rehandshake; acceptance strengthened to assert the engine peer gains the endpoint (UAPI get) AND an initiation packet actually egresses — rehandshake-counter alone declared insufficient.
  - **T70** (M21): resolved the active-endpoint identity ambiguity by specifying SPEC-SCOPED identity `(specIdx, AddrPort)` instead of a bare AddrPort value (cross-spec duplicates are legal at runtime since T67's dedup is textual-only; dedup-on-flatten was rejected to keep `check()`'s flattened-order semantics untouched); added acceptance case (6): hostname re-resolving onto another spec's literal standby causes no spurious re-map and zero SetPeerRemote.
  - **T77** (M22): scenario gains phase (0) boot-while-unresolvable → responder comes up → first-resolve installs endpoint → first handshake, proving the boot-unresolvable→first-resolve→handshake path end-to-end (the reviewer noted it was otherwise never exercised); acceptance updated to match.
- **Discovered:** R70's `defects[]` bucket is absent/empty — nothing to file. R70 was a reconciled 2-reviewer panel (opus go-ahead, fable revise; strictest-wins).
- **Issues:** none. Plan is ready for re-review.
