# plan-advance candidate — goal G4 — planner alias `opus` (claude:opus-4.8[1m])

Round: /cq:advance → chained /cq:plan:advance, multi-planner CANDIDATE mode (generate-N-then-judge).
Outcome: candidate plan emitted — 5 milestones / 20 tasks. No ledger writes (candidate mode).
Synthesis: SELECTED as the base candidate; persisted (with fable fold-ins) as M23–M27 + T80–T99
by the orchestrator.

### Session summary
- **Did:** Grounded read-only against G4's state and the actual `internal/bind`, `internal/device`, `internal/config`, `internal/reseq`, `internal/telemetry`, `internal/fec`, `internal/metrics` code; confirmed all Q21–Q28 answers and every cited singleton/seam.
- **Achieved:** Emitting a candidate task-DAG of 5 work milestones / 20 tasks: config foundation → behavior-preserving de-singletoning → authenticated path->peer demux/bootstrap/roaming/limits → device+metrics integration → isolation/threat/FEC/docs verification.
- **Discovered:** A clean single-`ReceiveFunc` design survives multi-peer by stamping each delivered inner datagram with its peer's virtual endpoint (per-packet endpoint), and `Send` demuxes via an endpoint->peer map — so the engine keeps A1's "one virtual endpoint per peer" without a new receive goroutine per peer. The concentrator's local sockets are shared across peers while each peer owns its return-path/remote view, which the binding table resolves.
- **Issues:** none blocking. The concentrator path/remote model (shared sockets vs per-peer path set) is the sharpest design point for the reviewer to scrutinize; the DAG isolates it inside the B/C milestones. Real-link (realhosts) verification is report-only and infra-gated per M10/Q12.
