# investigate-prober session summary — D35 / H1

- defect: D35 (allowed_ips=0.0.0.0/0 wedges the WG handshake)
- hypothesis: H1 (D35-H-engine — engine allowed-ip trie zero-length-prefix defect)
- role: prober (isolation=worktree, base 60138a6; hardware repro on o3 aarch64 + llm-ubuntu-0 amd64)
- lean: **contradicts** (H1 empirically refuted on both arches)

## Session summary
- **Did:** Built a minimal 2-peer amneziawg-go v1.0.4 handshake harness (stock conn.NewDefaultBind, plain-WG, amnezia obfuscation off) driving a literal allowed_ip into the UAPI via IpcSet, and ran the 3-arm netns repro (literal 0.0.0.0/0 vs 10.10.0.1/32 vs 0.0.0.0/1+128.0.0.0/1 split) with verbose engine logging + tcpdump on the receiver, on BOTH llm-ubuntu-0 (amd64) and o3 (aarch64 — the original-incident arch). Pinned the tested engine version and read the responder handshake path in receive.go/send.go and wanbond's UAPI render in device.go.
- **Achieved:** 5 numbered evidence items; lean **contradicts** (D35-H-engine refuted).
- **Discovered:** A LITERAL 0.0.0.0/0 allowed-ip does NOT wedge the amneziawg-go handshake. On both arches all three arms behaved identically: ConsumeMessageInitiation→Received handshake initiation→SendHandshakeResponse→Received handshake response, established in 1-3s, symmetric bidirectional byte flow (~1.7-1.9KB each side, ~28-31 UDP pkts), NO receiver rx-flood/tx≈0 wedge. No divergence point between arms. Pinned engine v1.0.4 (matches repo go.mod, no version skew). The receiver path never consults the allowed-ips trie before answering (receive.go:412-417).
- **Issues/caveats:** harness used stock plain-UDP conn.NewDefaultBind (not wanbond's multipath/FEC bind) and amnezia obfuscation off — refutes the ENGINE-trie mechanism but does not exercise wanbond's bind or a kernel-routing loop. LEADING UNREFUTED ALTERNATIVE: a kernel default-route loop (a literal /0 route installed without excluding the concentrator underlay endpoint → encrypted UDP recurses into the tunnel). Orchestrator follow-up (current HEAD): wanbond's own route programming (route_linux.go T108) installs the /1 split, NOT a literal /0, specifically to avoid overriding the underlay route — so the loop can only come from EXTERNAL route management, not wanbond code.
- Harness left at /home/ubuntu/d35probe on both throwaway hosts; netns cleaned.

Evidence highlights (full evidence-json in the raw transcript):
1. llm-ubuntu-0 amd64, arm literal /0: ESTABLISHED_AT=3s, rx=1716 tx=1628, 28 pkts — identical to /32 and split arms.
2. o3 aarch64, arm literal /0: ESTABLISHED_AT=3s, rx=1896 tx=1720, 31 pkts — identical to /32 and split arms.
3. amneziawg-go@v1.0.4 receive.go:409-417 — ConsumeMessageInitiation→SendHandshakeResponse without consulting the allowedips trie.
4. wanbond device.go allowed_ip render + no direct kernel-route from allowed-ips at base 60138a6 (superseded at HEAD by route_linux.go T108, which installs the /1 split).
5. go list -m confirms pinned amneziawg-go v1.0.4 == repo go.mod on both hosts.
