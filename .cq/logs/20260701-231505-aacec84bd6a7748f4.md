# plan-advance session — goal G1

- goal: G1 (wanbond: 2-WAN bonding tunnel with adaptive FEC on amneziawg-go + custom conn.Bind — implementation and test harness)
- role: planner (plan-advance, fresh-goal clarifying bootstrap via /cq:plan)
- milestone: M1
- returned status token: `awaiting-answers`
- agent-id: aacec84bd6a7748f4

Filed 8 clarifying questions (Q1–Q8) under M1, all `open` and linked to `goals:G1`. Goal G1 remains in `clarifying`; the server gate (no phase exit while linked questions are open) now holds until the user answers. Deliberately not asked: base-architecture choices (closed per the prompt), PSK-auth primitive selection, MTU/MSS strategy, reorder-buffer sizing — those are planner-decidable within the decided architecture.

### Session summary
- **Did:** Rule 2(a) for fresh goal G1 in `clarifying` — read `fetch_item("goals","G1")`, the authoritative `/home/pavel/work/safe/fecalia/fec-prompt.md`, and confirmed the workspace is greenfield (only `.cq/ledgers.yaml` + the prompt; not a git repo); filed the first batch of clarifying questions on the `questions` ledger under M1.
- **Achieved:** Created Q1–Q8, each with context/suggestions/recommendation and `ledgerRefs: ["goals:G1"]`:
  - Q1 — concrete verification thresholds for the prompt's N/X/Y/Z placeholders (P1 recovery time, P3 loss/recovery/overhead, P2 aggregation tolerance, P4 adaptive bound); proposed defaults offered.
  - Q2 — test-environment ground truth: netns+netem with root locally? real Starlink/5G edge + VPS available, or emulation-only verification?
  - Q3 — CI expectations and layering of privileged (netns/TUN) vs unprivileged tests.
  - Q4 — repo/toolchain conventions: Go module path, Go version, golangci-lint, Nix flake, git init in place.
  - Q5 — config format and shape (TOML vs INI vs YAML; explicit role field; inline secrets).
  - Q6 — deployment scope: systemd units/docs at which phase; amd64/arm64 targets; packaging.
  - Q7 — observability export surface (logs only vs Prometheus endpoint vs status CLI) and when it lands.
  - Q8 — planning depth: full P0–P5 DAG now vs P0-first re-plan; P0 spike timebox.
- **Discovered:** Pure greenfield — no existing code, module, CI, or git repo, so every tooling convention is genuinely open; goal description in the ledger faithfully mirrors `fec-prompt.md` (no contradictions found).
- **Issues:** none — goal correctly gated in `clarifying` awaiting user answers.

awaiting-answers
