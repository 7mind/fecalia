# plan-advance candidate — goal G5 — planner alias `fable` (claude:fable-5)

Round: /cq:plan:advance G5, multi-planner CANDIDATE mode (generate-N-then-judge, Q100/Q101).
Outcome: candidate plan emitted — 3 milestones / 12 tasks. No ledger writes (candidate mode).
Synthesis: SELECTED as the base candidate; persisted (with opus fold-ins) as M20/M21/M22 +
T67–T79 by the orchestrator.

### Session summary
- **Did:** CANDIDATE-mode pass for G5 — read the goal, all eight answered questions (Q29–Q36), and grounded the refactor surface via codegraph (config resolveEndpoints, bind ParseEndpoint/SetPeerRemote, device hubFailover/Close, bind reconcile loop, realhosts runner). Wrote nothing to any ledger.
- **Achieved:** A 12-task, 3-milestone candidate DAG honoring the answered decisions: opt-in overloaded endpoints (Q35/Q29), defer-and-reconcile boot (Q30), poll+liveness-loss re-resolution with change suppression via SetPeerRemote (Q31), multi-record expansion feeding hub-failover (Q32), first-class DoH+DoT resolvers behind an injectable seam (Q33), a mutable spec-keyed hubFailover endpoint set under its existing lock (Q34), and the netns dial-by-name/IP-change e2e as the v1 acceptance bar with realhosts as a report-only stretch (Q36).
- **Discovered:** (1) Keeping `Multipath.ParseEndpoint` IP-only is possible and desirable — the device layer can hand only resolved AddrPorts to the engine, so no hostname ever reaches the bind and the datapath stays untouched. (2) `startHubFailover` currently skips peers with `len(Endpoints) < 2`, so the wiring condition must change for a single-hostname peer (which may expand to N records) while a single-IP-literal peer must stay controller-less for byte-for-byte identity. (3) Go's netns e2e dial-by-name needs care: /etc/hosts is not netns-scoped, so the e2e needs a netns-local DNS server or a mount-namespaced hosts file — reflected in the e2e task's tier.
- **Issues:** DoH/DoT (Q33) and the mutable failover list (Q34) materially enlarge v1 scope versus the recommendations; the DAG isolates them (T3/T4 parallel; T6 a single frontier task) so a judge can weigh trimming them. Certificate-trust injection for DoH/DoT tests is called out inside those tasks to avoid an insecure-skip production knob.
