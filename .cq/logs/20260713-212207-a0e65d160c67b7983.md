# G5 plan bootstrap — planner (plan-advance), returned: awaiting-answers

Goal: G5 "Optional DNS (hostname) concentrator endpoints" (milestone M19). Fresh
goal, DEFAULT single-planner clarify-first step.

- **Did:** Grounded read-only against the cited code, then filed the FIRST
  clarifying-question batch (no plan, no decision).
- **Achieved:** Q29–Q36 under M19, each linked goals:G5, each grounded + suggestions
  + recommended default. Topics: (Q29) opt-in/default-off + IP-literal byte-identity;
  (Q30) defer-and-reconcile vs hard-resolve at load; (Q31) re-resolution
  trigger/cadence/repoint + no-op suppression; (Q32) multi-record A/AAAA + v4/v6
  preference; (Q33) resolver privacy + testable security acceptance target; (Q34)
  hub-failover interaction/coordination; (Q35) config surface (overload vs new
  field); (Q36) e2e/unit acceptance bar + realhosts scope. Goal remains clarifying.
- **Discovered:** Premises hold (ParseAddrPort rejects hostnames at config.go:495 +
  multipath.go:1327; metrics/server.go:133 proves DNS capability via net.LookupIP).
  Two non-obvious findings: (1) SetPeerRemote (multipath.go:1371) is deliberately
  disruptive — calls resequencer.Rebaseline() (D32) + caller re-handshakes — so a
  re-resolution repoint MUST suppress no-op (unchanged-IP) repoints or it drops the
  session needlessly; (2) hubFailover (failover.go:72-97) holds endpoints as an
  IMMUTABLE snapshot with active idx and also calls SetPeerRemote, so an independent
  re-resolution loop repointing the active endpoint RACES the failover controller
  (Q34). T55 tolerant-startup (deferredPath + StartReconcileLoop, reconcile.go:60-124,
  ~1s cadence) + the device stop-closure loop idiom (device.go:248-260/664-682) give
  the wiring model for the deferred/re-resolution loop.
- **Issues:** None blocking. Q34 (failover coordination) + Q35 (config surface) most
  shape the eventual DAG blast radius. Re-enters the planner for a grounded plan once
  the user answers.
