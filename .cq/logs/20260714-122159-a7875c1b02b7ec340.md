# investigate-explorer summary — D65 / H7 (H-D: bufferbloat / internal queue)
- defect: D65 · hypothesis: H7 · role: explorer · lean: contradicts

### Session summary
- **Did:** gathered evidence for H7 (H-D: oversized/unbounded internal queue with drop-tail).
- **Achieved:** 10 evidence items; lean contradicts.
- **Discovered:** wanbond has NO internal software TX queue — Send writes synchronously to the UDP socket; backpressure propagates to the WG engine. Pacer (weighted-only) SHEDS at the head (not tail-drop) and, when enabled, is BDP-sized ("without building a standing queue"). Receive resequencer bounded in memory (2048) and latency (250ms HOL cap), skips (advanceTo) rather than tail-drops. The ~1s loaded RTT is therefore EXTERNAL (Starlink link buffer / kernel qdisc). REAL sub-finding: under DEFAULT active-backup, wanbond applies NO egress pacing/AQM, so it neither causes an internal queue nor mitigates the external one.
- **Issues:** Runtime localization (tc -s qdisc, reseq HighWater, ping-under-load) needed to confirm where the 1s forms — external hardware.
