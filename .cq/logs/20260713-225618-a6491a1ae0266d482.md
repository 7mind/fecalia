# /cq:plan:advance G6 — plan-advance (default mode, opus frontier tier) — token: awaiting-answers

Goal G6 (production-edge operability & full-tunnel hardening, wanbond-fixes.md I1-I8 + C1-C6), coordination milestone M29. First planner pass on a fresh `clarifying` goal: filed the initial clarifying-question batch Q37-Q43. Configured planner panel (opus+fable, generate-N-then-judge) was NOT fanned out this round: a clarifying goal with unanswered scope questions cannot yield candidates, so the question-filing step ran as the default single-planner pass per the awaiting-answers provision.

### Session summary
- **Did:** Filed the first clarifying-question batch for G6 (state: `clarifying`, no prior questions, not defect-seeded). Grounded read-only via codegraph on the config surface first.
- **Achieved:** Created Q37-Q43 under M29, each `ledgerRefs: ["goals:G6"]`, with context + suggestions + a recommendation. They gate the seven genuine plan-shaping forks: (Q37) D35-D40 defect-gating/sequencing; (Q38) I7 code vs docs-only; (Q39) I8 verify-here vs investigate-flow defect; (Q40) C1/C4 docs-only vs packaged NM drop-in + oneshot unit; (Q41) I6 automation boundary vs C3/C6 manual recipes; (Q42) I5 config surface (per-path vs global `bind`); (Q43) all-in-one vs must-have first round. No phase transition (correct — open questions must be answered first).
- **Discovered:** wanbond config is a plain Go TOML struct (`internal/config/config.go`: `Config`/`Path`/`Peer`, `allowed_ips` on `Peer`, `source_addr` on `Path`) — NOT baboon-versioned, so I5/I6 config additions carry no external-API versioning cost; that de-risks those and is folded into the recommendations. Device-bind decision lives in `selectDeviceBinds` (`internal/bind/pathsock.go`), the exact site I5's toggle would override. D35-D40 (M28) are genuinely unlinked to G6 by ledgerRefs, confirming G6 is not defect-seeded.
- **Issues:** none blocking. The batch is deliberately broad (7 questions) because the goal itself enumerated these forks and they each shape the task DAG; the user's answers to Q37/Q41/Q43 in particular determine gating edges and whether G6 can reach `planned` independently of the D35-D40 investigations.
