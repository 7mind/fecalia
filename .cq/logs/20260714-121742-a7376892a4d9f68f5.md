# Plan-advance session — G13 (first clarifying round)

- **goal:** G13 (milestone M50) — "Operability & safety for weighted aggregation + pacing"
- **role:** planner (plan-advance, SINGLE-planner mode)
- **returned status:** awaiting-answers
- **agent-id:** a7376892a4d9f68f5
- **filed:** Q51–Q55 (questions ledger, milestone M50, status open, ledgerRefs goals:G13)

## Session summary (verbatim from planner)

**Did:** Read G13's full state (and grounded against the actual repo: weighted scheduler internals, pacer/FrameClass model, metrics Source/Collector surface, SizePacingFromBDP, and G2's task breakdown) and filed the FIRST batch of clarifying questions.

**Achieved:** Created Q51–Q55 (`questions` ledger, milestone M50, status `open`, `ledgerRefs:["goals:G13"]`, author `opus-4.8[1m]`). G13 remains in `clarifying`. Status token: `awaiting-answers`.

**Discovered:** (1) The empirical "38% ICMP loss" is almost certainly INNER-tunnel ICMP, which is opaque `ClassData` to the pacer — wanbond architecturally cannot give it a priority lane; the addressable, wanbond-visible target is PROBE-frame (`KindProbe`) starvation causing spurious path-down (Q51). (2) G2's Q20 pacing-sizing decision (auto-wire `SizePacingFromBDP` + BDP docs) is recorded but UNMATERIALIZED — G2's M14/M16/M17 are empty — so the goal text's "reference the G2 task instead of restating it" has no task to reference; the dedup must be settled at goal-ownership level (Q53). (3) The weighted scheduler already computes all aggregation-gate state (`s.aggregating`, EWMA `loadRate`, engage/disengage thresholds) but exposes none of it to the metrics collector, and it is a per-PEER object — so `wanbond_aggregation_engaged` cardinality is a real design decision (Q54).

**Issues:** None blocking. The plan cannot be fine-grained until these five scope/feasibility questions are answered — notably Q51 (feasibility boundary of the item-3 priority class) and Q53 (G2/G13 ownership split), which materially change the task DAG.
