# plan-reviewer — goal G5 — reviewer alias `fable` (claude:fable-5), configured mode

Round: /cq:plan:advance G5, multi-reviewer panel, round 1 (plan M20/M21/M22, T67–T79).
Returned verdict: revise (2 criticisms / 0 new_questions / 0 defects). Reconciled into R70
(strictest-wins → revise; both criticisms carried into R70 tagged [fable]).

### Session summary
- **Did:** reviewed the emitted plan for goal G5 (M20/M21/M22, T67–T79) against the canonical rubric; verified all cited symbols/lines via codegraph (config.go, multipath.go, failover.go, device.go), the e2e/realhosts harnesses, and go.mod
- **Achieved:** verdict revise; mode B → json returned, no ledger writes; 2 criticisms / 0 new questions / 0 out-of-scope defects
- **Discovered:** engine peer endpoint is installed only via UAPI `endpoint=` → `ParseEndpoint`, so T74's "SetPeerRemote + rehandshake" first-resolve mechanism cannot emit the first initiation after an endpoint-less boot; T70's value-based active-identity tracking is ambiguous when a hostname resolves to a literal standby's AddrPort
- **Issues:** none — could not read the amneziawg-go module cache directly (no Bash/Glob in this tool set), but the repo-side evidence (ParseEndpoint being the sole engine-endpoint install path) suffices for the criticism as phrased
