# G4 plan bootstrap — planner (plan-advance), returned: awaiting-answers

Goal: G4 "Multi-peer (hub-and-spoke) concentrator support" (milestone M18). Fresh
goal, DEFAULT single-planner clarify-first step.

- **Did:** Ran the DEFAULT single-planner step for fresh goal G4 (rule 2(a)):
  grounded read-only against the cited code, then filed the first batch of
  clarifying questions.
- **Achieved:** Verified every premise the goal cites, then created Q21–Q28 (open,
  under M18, ledgerRefs: ["goals:G4"]) covering: scope boundary; per-peer-PSK model
  + config schema + single-peer back-compat; wire-format-change acceptability;
  demux bootstrapping (gate-vs-quarantine + trial-decode-across-peer-PSKs); roaming
  re-bind; concentrator resource limits/eviction/backpressure; threat model for the
  path→peer binding; per-peer /metrics labels. Goal left in `clarifying`.
- **Discovered:** Premises hold. config.Config.PSK is a single top-level key
  (config.go:42), config.Peer has no PSK field, config.validate requires the
  top-level psk — so per-peer PSK is a real schema change. device.Up builds exactly
  ONE bind.NewMultipath(cfg.Paths, cfg.PSK, ...) with one scheduler/prober
  set/Reflector — the datapath is structurally single-peer even though
  [[wireguard.peers]] and uapiConfig iterate all peers. Two hooks to build on: (1)
  multipath.handleInbound already learns a path's return remote ONLY from an
  authenticated PROBE (D9/D11) — the seed of an authenticated path→peer binding;
  (2) reseq.Rebaseline + the resync guard exist for the T57/Q18 edge-side
  single-active hub-failover, distinct from this goal's concentrator multi-peer.
- **Issues:** None; blocked on the user answering Q21–Q28. Questions are
  interdependent (Q24 demux-bootstrap, Q26 resource-limits, Q27 threat-model form
  one cluster; Q22 per-peer-PSK + Q28 metrics both may touch a new per-peer config
  field) — read the answers together at plan time.
