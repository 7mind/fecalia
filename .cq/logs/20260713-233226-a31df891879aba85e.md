# /cq:plan:advance G6 — plan-advance (default mode, opus frontier tier) — token: review-requested (R80 revision pass)

Goal G6 (M29), phase planning. Applied review R80's 3 criticisms to the plan (rule 3); phase unchanged; plan returns to the reviewer panel.

### Session summary
- **Did:** Applied review R80's three criticism entries (revise, empty new_questions) to the G6 plan; goal phase left unchanged at `planning`.
- **Achieved:** Updated T115 — dependsOn now `[T101, T105, T107, T108, T109]` (added T101 + T108 so the sync sweep cannot schedule before the metric names and default-route note it documents exist); added `docs/runbook.md` (C1/C3/C4 provisioning steps) to T115's sweep description and acceptance. Updated T101 — reworded the flaky-by-design e2e acceptance to assert path-up→session-established ordering from log transition timestamps rather than a scrape-observed path_up=1/session=0 intermediate state, keeping the 0→1 transition and exactly-once log assertions intact.
- **Discovered:** R80 carried no `defects[]` bucket, so no file-and-defer step was needed. The ~25 s path-up→session gap is a production/WAN artifact; the netns tier closes it in milliseconds, which is exactly why the scrape-based ordering assertion was nondeterministic.
- **Issues:** none — plan is revised and ready for re-review.
