# investigate-explorer summary — D65 / H5 (H-B: scheduler reorder)
- defect: D65 · hypothesis: H5 · role: explorer · lean: contradicts

### Session summary
- **Did:** gathered evidence for H5 (H-B: scheduler reorder overwhelms resequencer → TCP misreads as loss).
- **Achieved:** 7 evidence items; lean contradicts.
- **Discovered:** Default scheduler is active-backup (single active path carries ALL egress; others idle). Send writes each frame to exactly one socket (no fan-out/dup during failover). Resequencer releases in strict outer-seq order BEFORE WireGuard (window 2048 / 250ms), absorbing multipath reorder so TCP/WG never see it. Single-flow striping exists ONLY under non-default opt-in policy="weighted". Mechanism does not operate under default config.
- **Issues:** Cannot confirm which policy the D65 repro used; if weighted, item 6 becomes live.
