# implement-worker — task T80 (goals:G4) — model sonnet-5, round 1

Returned: status pass; resultCommit d47be13b4fd0dfb97e0ebbd1e82d65d0beeb810b; check green (go build/vet/test, 12 packages).
CONTRACT BREACH (validate_output): the returned json omitted the required `branch` field
(outputSchema requires it). Verified directly in the worktree: the worker DID create and commit
on `implement/T80` — the breach is in the reported json only, not the git state.

### Session summary
- **Did:** implemented task T80 in worktree `implement/T80` — added optional per-peer `psk` (`config.Key`) and `name` (`string`) fields to `internal/config.Peer`, with two new unit tests: `TestLoadPeerPSKName` (2-peer TOML, distinct per-peer psk+name) and `TestLoadSinglePeerLegacyPSKGoldenShape` (legacy single-peer TOML, golden `reflect.DeepEqual` compare).
- **Achieved:** pass; resultCommit `d47be13b4fd0dfb97e0ebbd1e82d65d0beeb810b`; `go build ./... && go vet ./... && go test ./...` green across all 12 packages.
- **Discovered:** the base commit SHA the orchestrator passed (`553ebc9b...`) is a descendant of the worktree's actual HEAD (`60138a6`) — a `git diff --stat` between them showed only `.cq/*` ledger files, `README.md`, `docs/install.md`, and `wanbond.example.toml` differ (no code under `internal/`), so it was safe to proceed without a base refresh. `Config.normalize()` needed no new logic: `Key.UnmarshalText` already treats an absent TOML key as unset, so the legacy single-peer shape stays byte-identical automatically. `SchedulerConfig.applyDefaults()` unconditionally sets `Policy = "active-backup"` on every load, which the golden-struct test had to account for.
- **Issues:** none. `wanbond.example.toml` / `docs/install.md` don't exist at this worktree's base commit, so not updated — fields are unconsumed groundwork for a later G4 task.
