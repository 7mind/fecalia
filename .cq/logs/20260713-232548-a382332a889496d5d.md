# /cq:plan:advance G6 — candidate planner [opus] (plan-advance, CANDIDATE mode) — candidate plan emitted

Goal G6 (M29). One of 2 parallel candidate planners (generate-N-then-judge). Emitted a 7-milestone / 16-task candidate. Judged: NOT the base (fable's chosen); folded in: I7 acceptance (stable ifindex + NM-unmanaged), NM drop-in packaging presence-check.

### Session summary
- **Did:** Grounded read-only in G6's state (answered Q37-Q43 under M29), wanbond-fixes.md, and the actual wanbond repo, then produced ONE candidate task-DAG for the full G6 scope. No ledger writes.
- **Achieved:** A 7-milestone / 16-task candidate covering I1-I8 + C1-C6 + the Q40 packaged artifacts, honoring the specific answers: I7 persistent-TUN CODE in scope (Q38 "Both"), I8 in-goal verification test (Q39), packaged NM drop-in + templated addressing oneshot (Q40), THIN I6 (Q41), per-path bind + global default (Q42), everything in one goal (Q43). Only I6's literal 0.0.0.0/0 acceptance gated on D35 (Q37).
- **Discovered:** No bind/mode/full-tunnel concept exists today; wanbond0 is created DOWN and non-persistent (no LinkSetUp, no TUNSETPERSIST); no handshake/session metric (only probe-plane wanbond_path_up); TUN-write EIO originates in the amneziawg-go dep and surfaces via wanbond's engineLogger adapter; errNoHealthyPath is returned to the engine and logged at ERROR — I3/I4 best implemented at the engine-log adapter / a TUN decorator in internal/device.
- **Issues:** I6's e2e default-route assertion and literal-0.0.0.0/0 acceptance gated on D35 resolving; candidate scopes I6 to the /1+/1 split so it is plannable now. I8 may refile a defect.
