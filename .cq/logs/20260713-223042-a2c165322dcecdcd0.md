# plan-advance candidate — goal G4 — planner alias `fable` (claude:fable-5)

Round: /cq:advance → chained /cq:plan:advance, multi-planner CANDIDATE mode (generate-N-then-judge).
Outcome: candidate plan emitted — 4 milestones / 11 tasks. No ledger writes (candidate mode).
Synthesis: NOT selected as base (opus's finer-grained candidate won); folded in: the explicit
pathState shared-socket vs per-(peer,path) decomposition, edge-role-with->1-peer rejection (Q21),
uapiConfig golden byte-identity acceptance, (peer,path)-keyed metrics rate maps, and lazy
per-peer state instantiation.

### Session summary
- **Did:** CANDIDATE-mode pass for G4 — read the goal, all 8 answered questions (Q21–Q28), and grounded the refactor surface via codegraph against internal/bind/multipath.go, internal/config/config.go, internal/device/{device,metrics,failover}.go.
- **Achieved:** Emitted a candidate task-DAG: 4 work milestones, 11 tasks (config surface → bind de-singleton + authenticated demux → device/metrics wiring → verification/docs), each with operational acceptance and model tiers. No ledger writes.
- **Discovered:** The per-path `remote`/`prober` state must split per-(peer,path) — concentrator paths (sockets) are SHARED across peers, so `pathState` needs decomposition into shared socket state vs per-(peer,path) telemetry/routing state; the reflector must also trial-verify inbound probes per-peer since PSKs become per-peer. Edge role with >1 peer should be rejected by validate (Q21 scope).
- **Issues:** none — candidate returned, judge decides.
