# plan-advance — goal G4 — DEFAULT single-planner mode (revision pass, tier frontier)

Round: /cq:advance → chained /cq:plan:advance, revision applying reconciled review R72 (revise, 2 criticisms).
Returned status token: review-requested.

### Session summary
- **Did:** Rule 3 (revise on criticism-only `revise` review R72) for G4 — revised three tasks in place, no phase change.
- **Achieved:** (1) T89.dependsOn now `["T88","T86"]`, putting T86's per-peer resequencer demux in the transitive closure of T89, T90 (→T89), and T92 (→T90/T91→T89) with a single edge. (2) Runtime path add/remove scoped IN, not deferred: T83 (bind side) now requires the shared-path add/remove fan-out to instantiate/tear down per-(peer,path) state for every bound peer, with a new >=2-peerState add/remove unit-test acceptance plus the existing runtime_path_test.go / tolerant_membership_test.go suites passing; T93 (device side) now requires bind.ProberFactory to key each (peer,path) prober on that peer's effective PSK (replacing the device.go:577 closure over the single cfg.PSK), with a runtime-add-while-2-peers-bound acceptance.
- **Discovered:** The single T89→T86 edge fixes all three flagged acceptances transitively — no need to touch T88, T90, or T92's edges. Scoping runtime path mutation IN (rather than declaring it out of scope) was the sound choice because the deferred-path machinery T83 already names lives on exactly the shared socket state the split creates.
- **Issues:** none.
