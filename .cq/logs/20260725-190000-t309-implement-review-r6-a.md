# T309 implementation review R6 A

Verdict: **approve**

Reviewed final code commit: `8e0161e3508bbc9594710f1d7faf312b82b179f1`

The final whole-bind Close race is fixed and deterministically covered:

- after socket retirement and `readersWG.Wait`,
  `internal/bind/multipath.go:3796-3802` performs a final sender detach and
  joins outside `m.mu`;
- detach atomically swaps `fecSend` to nil and stops captured owners at
  `:3807-3819`; join occurs at `:3829-3835`;
- `TestFailFirstFECWholeCloseRejectsLateLazyOwnerPublication` forces publication
  after the initial detach, requires Close to return with `fecSend=nil` and the
  captured owner exited, then reopens with a distinct owner.

The reviewer reconfirmed one-batch ownership, owner-side sequence assignment,
publication/stop acknowledgement, complete-group handoff, adaptive lock order,
and peer teardown serialization. Focused fail-first ordinary/race and
bind/FEC ordinary/race tests passed.
