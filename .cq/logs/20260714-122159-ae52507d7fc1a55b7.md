# investigate-explorer summary — D65 / H6 (H-C: CPU-bound send path)
- defect: D65 · hypothesis: H6 · role: explorer · lean: insufficient (probeRequest)

### Session summary
- **Did:** gathered evidence for H6 (H-C: CPU-bound serialized codec+FEC send path).
- **Achieved:** 8 evidence items; lean insufficient.
- **Discovered:** Every structural inefficiency confirmed — encode serialized under one m.mu; sendCodec.Encode(nil,...) allocates per frame (reuse seam unused); FEC Admit double-copies; per-frame XChaCha20 + HMAC-SHA256; per-group RS alloc+encode; multipathBatchSize=1, one sendto per frame, no GSO/sendmmsg. CONTRADICTING: NO internal userspace send queue that could tail-drop (Send is synchronous); only deliberate drop is the pacer's PickPaced shed. 3.67 Mbps is 1-2 orders below plausible aarch64 crypto+RS ceiling → CPU implausible as sole cause.
- **Issues:** No encode/send benchmark exists; causation needs a CPU+alloc profile / per-core encode ceiling number. probeRequest attached.
