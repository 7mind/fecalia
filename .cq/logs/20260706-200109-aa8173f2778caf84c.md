# T8 review — implement-reviewer fable (round 1) (agent aa8173f2778caf84c)

Task: T8 (wanbond P0 tunnel bring-up), goal G1. Verdict: **disapprove**.

4 criticisms: exit-0 on engine death; stale 'wanbond version' doc; factually wrong writeAmnezia comment; e2e log-buffer data race (+ cmd.Process.Wait vs cmd.Wait). Out-of-scope defect: amneziawg-go stores amnezia message types in package-level globals (T19 constraint).

(raw transcript unavailable: `cq log put` rejected cq.toml [tiers] schema — installed cq CLI/config version skew, unrelated to wanbond; redaction path could not run.)
