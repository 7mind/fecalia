# T8 review — implement-reviewer opus (round 1) (agent a1fd7a439122cc6ad)

Task: T8 (wanbond P0 tunnel bring-up), goal G1. Verdict: **disapprove**.

Bring-up/UAPI/teardown sound; e2e cannot pass spuriously (10.10.0.2 reachable only through the TUN). Blocking: daemon exits 0 on unexpected engine death (masks failure from supervision). Out-of-scope defect: partial-amnezia h1..h4=0 footgun (defer to T19).

(raw transcript unavailable: `cq log put` rejected cq.toml [tiers] schema — installed cq CLI/config version skew, unrelated to wanbond; redaction path could not run.)
