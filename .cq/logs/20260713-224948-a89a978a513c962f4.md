# implement-worker — task T67 (goals:G5) — model sonnet-5, round 1

Returned: status pass; resultCommit c6f9235b8381b3db1bfa3b2aed0a7d538bcac0f8; branch implement/T67; check green (go build/vet/gofmt/test + golangci-lint).

### Session summary
- **Did:** implemented task T67 in worktree `implement/T67` — overloaded `Peer.resolveEndpoints` (`internal/config/config.go`) to accept hostname endpoints behind a new per-peer `dns = true` opt-in, adding the `EndpointSpec` typed representation and `Peer.EndpointSpecs`/`Peer.DNS` fields, plus adjusted `validate()`'s edge/concentrator endpoint checks to use `EndpointSpecs` instead of `Endpoints`.
- **Achieved:** pass; `resultCommit` c6f9235b8381b3db1bfa3b2aed0a7d538bcac0f8; full gate green (`go build ./... && go vet ./... && gofmt -l cmd internal test && go test ./...`), plus `golangci-lint run ./internal/config/...` clean.
- **Discovered:** `validate()` previously gated the edge "endpoint required" / concentrator "not meaningful" checks on `len(peer.Endpoints)`, but a hostname-only peer contributes nothing to `Endpoints` at load time (resolution is deferred to runtime per Q30) — had to retarget those checks at `EndpointSpecs` so a valid hostname-only edge peer isn't spuriously rejected, and so a concentrator declaring `dns = true` with zero endpoint entries is still caught. Also added a guard so a malformed IP-shaped entry (e.g. bad port) is diverted back to the original `ParseAddrPort` error rather than misleadingly reported as "requires dns=true". `internal/bind`'s `TestMultipathFECDeadlineEmitsPartialGroupParity` is flaky/timing-sensitive under load (failed once, passed 5/5 in isolation) — confirmed unrelated to this change (not touched, pre-dates it).
- **Issues:** none. Doc sweep explicitly deferred to T79 per the plan; T67's acceptance is config-parsing only.
