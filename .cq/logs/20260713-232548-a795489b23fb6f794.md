# /cq:plan:advance G6 — candidate planner [fable] (plan-advance, CANDIDATE mode) — candidate plan emitted

Goal G6 (M29). One of 2 parallel candidate planners (generate-N-then-judge). Emitted a 4-milestone / 16-task candidate. Judged: BASE candidate (surface-then-wiring splits of I5/I6, opt-in tun_persist, docs-coupled-to-packaging, two-sided I8 test, reference-sync sweep). Synthesis persisted by the orchestrator as M30-M33 / T100-T115.

### Session summary
- **Did:** CANDIDATE-mode pass for G6: read the goal, answered questions Q37-Q43, wanbond-fixes.md, and grounded in the repo via codegraph (device lifecycle, path-socket bind planning, engine logger/no-healthy-path error, metrics exposition seam, config surface, install.md structure, packaging/systemd layout).
- **Achieved:** Emitted one candidate task-DAG: 4 work milestones, 16 tasks covering I1-I6 code, I7 persistent-tun code + C4 oneshot (Q38 both), the I8 in-goal verification test (Q39), packaged NM drop-in + templated addressing oneshot (Q40), thin I6 boundary (Q41), per-path+global bind mode (Q42), all C1-C6 docs (Q43), with D35/D36/D37/D39 referenced only in acceptance text per Q37 (no hard dependsOn on defect fixes).
- **Discovered:** The engine's "no healthy path" spam originates as errNoHealthyPath (internal/bind/multipath.go:64) surfaced through engineLogger Errorf (internal/device/device.go:687) — I4 needs a warmup-state seam between bind and engine logger, not a log-level change. deviceRehandshake (internal/device/failover.go:181) already exists as the fresh-handshake primitive T57 built, useful context for I2's session signal. Daemon currently programs no addresses/routes at all (install.md §4 states this as a design property), so the default-route wiring is the first route-programming code — flagged frontier.
- **Issues:** none blocking; the literal-0.0.0.0/0 pass-through remains gated on D35 by acceptance wording only, and C1/C4 doc content is deliberately coupled into the two packaging tasks (AGENTS.md docs-with-code rule) rather than standing alone.
