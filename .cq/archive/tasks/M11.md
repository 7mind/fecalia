## M11

### T42 — done

- createdAt: 2026-07-08T21:03:02.311Z
- updatedAt: 2026-07-08T21:39:12.492Z
- author: "opus-4.8[1m]"
- session: 45fdce95-2af6-42cd-8ddd-0c9faabc56ef
- headline: "Harden e2e/netns test harness: LISTEN-poll readiness, idempotent Setup/synchronous Teardown, engine_test goroutine leak (D3, D14, D20)"
- description: |
    Group A, test-only harness robustness (low sev). Three coupled fixes in the e2e/netns harness + a bind unit test:
    
    D3: test/e2e/p0_test.go iperf3Mbps (500ms) and test/e2e/baseline_test.go rttUnderLoad (800ms) start a one-shot `iperf3 -s -1` server then sleep a FIXED interval before the client connects, racing a slow bind into 'connection refused' under load. Add a SHARED readiness helper that polls for the LISTEN socket WITHOUT connecting (a probe-connect would consume the `-1` server's single accept and break the real client) -- e.g. `nsenter -t <pid> -n ss -ltn 'sport = :<port>'` or read /proc/net/tcp in the netns until LISTENING, with a bounded deadline -- and use it at both call sites in place of the sleeps.
    
    D14: test/e2e/netns.go Setup creates fixed-name veths (wbAe/wbBe) but Teardown returns before the prior namespace/holder-process is reaped, so back-to-back `go test -tags e2e ./test/e2e` runs fail with `RTNETLINK answers: File exists`. Make Setup idempotent (pre-delete any leftover wbAe/wbBe links before create -- generalize T26's SetupWithPaths pre-delete) OR make Teardown synchronously wait for holder-process + link removal before returning.
    
    D20: internal/bind engine_test.go ~L99 TestMultipathEngineUpCanTransmit's helper does an unbuffered channel send with no done escape, leaking the producer goroutine after the receiver is gone. FIX: use a buffered channel (cap 1) or `select { case ch<-v: case <-done: }`. In ADDITION, add an explicit goroutine-leak assertion so the fix is verifiable (see acceptance): `go.uber.org/goleak` -- `goleak.VerifyTestMain(m)` in the package TestMain OR `defer goleak.VerifyNone(t)` inside the test -- OR a `runtime.NumGoroutine()` before/after delta with a bounded settle window. The assertion MUST fail against the current unbuffered-send code (it observes the parked producer goroutine) and pass after the buffered/select+done fix; a bare `-count` run cannot observe the leak, so it is not the gate.
    
    All test-only; no product-code change.
- acceptance: "Repo-side: `go vet -tags e2e ./test/e2e` and `go build -tags e2e ./...` compile; the D3 helper polls the LISTEN state (never probe-connects) and is used by both iperf3Mbps and rttUnderLoad. D20 leak GATE (non-vacuous, operational): the bind unit test carries an explicit goroutine-leak assertion -- `go.uber.org/goleak` via `goleak.VerifyTestMain(m)` in internal/bind TestMain OR `defer goleak.VerifyNone(t)` inside TestMultipathEngineUpCanTransmit, OR an explicit before/after `runtime.NumGoroutine()` delta with a bounded settle window asserting zero residual engine goroutines after shutdown. This assertion MUST FAIL on the CURRENT unbuffered-send code (detecting the producer goroutine parked on the `ctun.Outbound <-` send at internal/bind/engine_test.go:99) and PASS after the buffered-channel / select+done fix -- verify by confirming it flags the leak pre-fix, then goes green post-fix. `go test -run TestMultipathEngineUpCanTransmit -count=20 ./internal/bind` is green post-fix, but the -count run is NOT the leak gate (a leaked goroutine does not fail a passing `go test`); the goleak/NumGoroutine assertion is. Hardware-validation (netns fixture, manual/report-only per M10/Q12): on a real host (o3 or llm-ubuntu-0) run `go test -tags e2e ./test/e2e` TWICE back-to-back and confirm the second run does NOT fail with RTNETLINK 'File exists' (D14 idempotency) and iperf3 readiness no longer races under load."
- suggestedModel: standard
- ledgerRefs: ["goals:G1","defects:D3","defects:D14","defects:D20"]

### T43 — done

- createdAt: 2026-07-08T21:03:12.810Z
- updatedAt: 2026-07-08T21:36:57.036Z
- author: "opus-4.8[1m]"
- session: 45fdce95-2af6-42cd-8ddd-0c9faabc56ef
- headline: "Config/bind path-source correctness: reject duplicate source_addr at config load; exclude link-local from v6 device-bind family count (D10, D13)"
- description: |
    Group A, product-code path/bind correctness (low sev). Two coupled fixes:
    
    D10: internal/config/config.go validate() enforces unique path NAMES but not unique SourceAddr; internal/bind/multipath.go Open binds (SourceAddr, port) per path, so two paths sharing a source_addr collide with EADDRINUSE at the second ListenUDP (and on every re-Open after Down/Up, since the engine passes the fixed listen port back). Track seen SourceAddr values alongside names in validate() and reject duplicates at config LOAD with a per-path error naming BOTH conflicting paths.
    
    D13: internal/bind/pathsock.go interfaceInfo counts EVERY same-family address; an up interface virtually always carries a kernel fe80::/10 link-local, so a GLOBAL v6 source sees familyCount>=2 and selectDeviceBinds falls back to source-IP binding -- v6 uplink paths never get T16 re-roam survival. For a GLOBAL (non-link-local) v6 source, EXCLUDE link-local addresses from familyCount (the kernel never source-selects a link-local for a global destination, so device binding still provably preserves the source_addr pin).
- acceptance: "Repo-side unit tests: (D10) a config with two paths sharing source_addr is REJECTED at validate() with an error naming both path names; distinct source_addr still loads. (D13) interfaceInfo unit test with a synthetic address set {global-v6, fe80:: link-local} yields familyCount excluding the link-local so a global-v6 source qualifies for device binding; a link-local-only interface is unaffected. `go build ./...` + `go test ./internal/config ./internal/bind` green. Optional netns confirmation that a global-v6 source device-binds (report-only) if a v6-capable host is available."
- suggestedModel: standard
- ledgerRefs: ["goals:G1","defects:D10","defects:D13"]

### T44 — done

- createdAt: 2026-07-08T21:03:21.434Z
- updatedAt: 2026-07-08T21:39:13.985Z
- author: "opus-4.8[1m]"
- session: 45fdce95-2af6-42cd-8ddd-0c9faabc56ef
- headline: Add per-peer anti-replay to outer CONTROL/PROBE handling (ProbeSeq high-water + stale-timestamp rejection) (D4)
- description: "Group A, security-relevant product code (deferred from T13). internal/frame.Decode verifies the CONTROL/PROBE HMAC but is STATELESS, so a passively-captured valid authenticated frame replays with a passing MAC (a replayed CONTROL rekey or PROBE). This is correct for the stateless codec -- replay defense belongs in the downstream probe/liveness + control handling state machine (the T13 layer), which already has the enabling fields exposed by the codec: Probe.ProbeSeq, Probe.TimestampNanos, Control.ControlType. Add per-peer replay rejection there: track a per-peer ProbeSeq high-water mark (reject non-advancing ProbeSeq) and/or reject stale TimestampNanos beyond a bounded skew window, and apply replay rejection to security-relevant ControlType messages. Preserve legitimate frame acceptance (monotonic ProbeSeq / fresh timestamps pass)."
- acceptance: "Repo-side unit test in the probe/liveness + control layer: a captured valid-MAC PROBE re-injected with a non-advancing ProbeSeq (or a stale TimestampNanos beyond the skew window) is REJECTED as a replay; a fresh PROBE with advancing ProbeSeq / current timestamp is ACCEPTED; a replayed security-relevant CONTROL frame is rejected. `go test ./...` green (existing T13 behavior unregressed). D4 auto-resolves on merge (it ledgerRefs the fixing task)."
- suggestedModel: frontier
- ledgerRefs: ["goals:G1","defects:D4"]

### T45 — done

- createdAt: 2026-07-08T21:03:37.449Z
- updatedAt: 2026-07-08T21:43:26.635Z
- author: "opus-4.8[1m]"
- session: 45fdce95-2af6-42cd-8ddd-0c9faabc56ef
- headline: "FEC codec hardening: partial-m x partial-k byte-exact property test + PIN klauspost prefix-stability; count retained-past-deadline groups at Stats() (D25, D24)"
- description: |
    Group B, FEC correctness + observability. Two coupled fixes in internal/fec:
    
    D25 (MEDIUM): the adaptive datapath emits groups coded RS(K,m) with varying m and decodes against a FIXED RS(K,ceiling) decoder. This is byte-exact ONLY because reedsolomon@v1.14.1's DEFAULT buildMatrix (Vandermonde x top-inverse) makes parity shard j identical for RS(m,k) and RS(m,ceiling) -- an UNDOCUMENTED implementation detail, not a public API guarantee. A future minor bump flipping the default (Cauchy/PAR1/Jerasure/leopard, or fastOneParity for k=1) would SILENTLY corrupt every reconstructed inner datagram with no test catching it. Also TestVaryingParityDecodesAtCeiling only round-trips FULL groups (m=DataShards=10) with varying k; the (partial-m x partial-k) space the adaptive encoder actually produces (deadline-flushed groups where m<DataShards AND k<ceiling) is untested. FIX: (1) extend the property test to cover m in [1,DataShards] x k in [0,ceiling] with byte-exact recovery through the single ceiling decoder; (2) PIN the guarantee -- assert at build/test time that the constructed generator-matrix parity rows are a stable prefix as total-parity varies, AND add a go.mod version-pin note + doc comment that reedsolomon must stay on a version whose default New() uses the Vandermonde buildMatrix (re-verify on any upgrade).
    
    D24 (low): internal/fec/decoder.go evictStale (~L232-242) accounts a failed group as unrecoverable ONLY when the fecRetainGroups=512 high-water advances past it, and the high-water advances only when NEW groups are offered. At quiescence/link-stall the last ~512 doomed groups are never counted, so wanbond_fec_unrecoverable_packets_total under-reports and /metrics OVERSTATES recovery after an incident. FIX: account retained-incomplete groups whose recovery deadline has DEFINITIVELY passed (past the point more parity could arrive) at Stats()/snapshot time WITHOUT evicting them from the reconstruction buffer (or add time-based eviction alongside the 512-group window). Count each group exactly once -- avoid premature/double counting.
    
    ORDERING (advisory, per review R39): land T45 BEFORE T46. T45 installs the build/test-time klauspost generator-matrix parity-prefix-stability assertion + the partial-m x partial-k byte-exact property test that prove ANY varying-M group decodes at the fixed ceiling. T46 (target_residual) widens the range of M the adaptive encoder emits, so T45's byte-exact decode guarantee should be proven before T46's widened M range is produced. This is advisory priority (the varying-M mechanism PRE-EXISTS per D25 and is already correct), not a hard dependsOn -- consistent with M11's 'tasks technically independent; advisory priority, not a hard dependency' model.
- acceptance: "Repo-side, `go test ./internal/fec` green with: (D25) a property test that round-trips m in [1,DataShards] x k in [0,ceiling] through the fixed ceiling decoder with byte-exact payload recovery; a build/test-time assertion that FAILS if the reedsolomon generator-matrix parity-prefix stability invariant is violated; a go.mod version-pin note + doc comment recording the required matrix default. (D24) a unit test where groups fail at quiescence (no further groups offered) and, after the recovery deadline passes, Stats()/snapshot reports them in wanbond_fec_unrecoverable_packets_total exactly once (recovered fraction no longer overstated), with no double counting when groups are later evicted."
- suggestedModel: frontier
- ledgerRefs: ["goals:G1","defects:D25","defects:D24"]

### T46 — done

- createdAt: 2026-07-08T21:03:49.934Z
- updatedAt: 2026-07-08T21:41:04.755Z
- author: "opus-4.8[1m]"
- session: 45fdce95-2af6-42cd-8ddd-0c9faabc56ef
- headline: "Adaptive-FEC: derive redundancy from a new target_residual config parameter (D26, per Q16 option i)"
- description: |
    Group B, adaptive-FEC control-law config surface. NOTE: this task ADDS a new product capability (a config surface) -- explicitly approved via Q16 (user chose option i) despite the round's general no-new-capability non-goal.
    
    Defect: adaptivefec DefaultSafetyFactor=1.5 / DefaultRaiseThreshold=0.05 cannot meet a sub-1% residual SLA -- at 5% loss with K=10 the default sizes M=1 giving ~1% residual (2x the P4ResidualLossMax=0.5% bound), and steady loss below 5% raises no parity at all. Nothing maps (target residual, K, loss) -> M for an operator; only the bare [fec].safety_factor multiplier is exposed.
    
    FIX (Q16 option i): add a NEW `target_residual` config parameter and DERIVE the redundancy map from it -- invert the binomial residual E[max(0,D-m)]/K (D ~ Bin(K, smoothed_loss)) to compute the minimum M meeting the target residual for the current smoothed loss and K. Make the residual target (not the bare SafetyFactor multiplier) the primary config surface; preserve/deprecate the existing safety_factor path coherently. Keep the control loop STABLE (respect existing hysteresis/raise-lower bands -- adaptive control-loop stability is the project's stated crux; do not introduce oscillation).
    
    ORDERING (advisory, per review R39): land T46 AFTER T45. T46 widens the range of M the adaptive encoder emits; T45 proves (build/test-time generator-matrix prefix-stability assertion + partial-m x partial-k byte-exact property test) that ANY varying-M group decodes byte-exact at the fixed ceiling. Do not emit a widened M range before T45's decode guarantee is in place, so a newly-widened M is never produced without proof it decodes correctly. Advisory priority, not a hard dependsOn (the varying-M mechanism PRE-EXISTS per D25 and is already correct) -- consistent with M11's advisory-ordering model.
- acceptance: "Repo-side: the new `target_residual` config field parses + validates (documented default + bounds; invalid values rejected at config load). Unit test: for representative (target_residual, K, smoothed_loss) the derived M yields a MODELED binomial residual <= target_residual (and M does not exceed the ceiling). The control loop shows no oscillation across a loss sweep (hysteresis preserved). The P4 adaptive-FEC e2e residual bound (P4ResidualLossMax=0.5%) is still met with target_residual set accordingly. `go test ./...` green. Ops/install doc notes the new parameter and its relation to the SafetyFactor path."
- suggestedModel: frontier
- ledgerRefs: ["goals:G1","defects:D26"]

### T47 — done

- createdAt: 2026-07-08T21:04:00.454Z
- updatedAt: 2026-07-08T21:55:49.561Z
- author: "opus-4.8[1m]"
- session: 45fdce95-2af6-42cd-8ddd-0c9faabc56ef
- headline: "Pacer: classify + priority/exempt WG control frames at the Bind; size per_path_capacity from measured BDP (D22)"
- description: "Group C (MEDIUM), pacing hardening. With pacing enabled, EVERY datagram -- data, WG handshake init/response, keepalive -- shares the same per-path Pick() token buckets, so under sustained overload control frames are shed at bulk-data probability (~80% at 5x overload), delaying rekey (mitigated only by WG's 5s REKEY_TIMEOUT retries within the 90s attempt window). Pick() has NO frame-type visibility. FIX: plumb frame-type classification from the Bind/interface down to the pacer so WG control frames (handshake/keepalive) are classified and EXEMPTED or given a priority class (a small reserved per-path token budget or a control-frame bypass), and SIZE per_path_capacity from measured BDP rather than the frame-count default (T21 default per_path_capacity_fps=10000 ~115 Mbit/s means the aggregation gate may never engage on realistic slow uplinks). This is Bind/interface frame-type plumbing that Pick() alone cannot provide; the codec (T11) already exposes frame types. Pacing ships DISABLED by default, so there is no default-config exposure, but a deployment enabling it needs rekey to survive overload."
- acceptance: "Repo-side unit/e2e test: with pacing ENABLED under sustained ~5x overload, WG control frames (handshake/keepalive) are NOT shed at the bulk-data drop probability -- the reserved budget / bypass keeps control-frame delivery high enough that rekey succeeds (no rekey starvation); bulk data is still paced. A test/doc shows per_path_capacity derived from measured BDP rather than the raw frame-count default. `go test ./...` (and the relevant `-tags e2e` pacing test on a netns/real host, report-only) green. Frame-type classification is plumbed Bind -> pacer without regressing the disabled-by-default path."
- suggestedModel: frontier
- ledgerRefs: ["goals:G1","defects:D22"]

### T48 — done

- createdAt: 2026-07-08T21:04:14.445Z
- updatedAt: 2026-07-08T21:32:48.919Z
- author: "opus-4.8[1m]"
- session: 45fdce95-2af6-42cd-8ddd-0c9faabc56ef
- headline: "Concentrator firewall: reboot-persistent ACCEPT rule provisioning + install-doc + TestRealProvision; live-o3 dedup+persist as manual ops step (D7, D8)"
- description: |
    Group D (D7 MEDIUM, D8 low), real-host/deploy. Q14 answer: o3 is a TEST host (live iptables edits/reboots are acceptable; NEVER deprovision/terminate it; implement-workers cannot reach o3 from their sandbox, so the live-host apply is a MANUAL report-only ops step, NOT an automated implement-worker gate).
    
    D7: T32's provision inserts `iptables -I INPUT -i wanbond0 -j ACCEPT` into the RUNTIME chain only; OCI Ubuntu restores /etc/iptables/rules.v4 at boot, so a reboot silently drops the rule and inbound tunnel TCP hits the default REJECT again. REPO-SIDE FIX: add an idempotent reboot-persistence provisioning step (netfilter-persistent save after insertion, or an idempotent edit of /etc/iptables/rules.v4, or a small systemd unit re-applying on boot), state-guarded so re-runs are no-ops; update T22's install doc; extend TestRealProvision to assert the PERSISTED rule set (when run against a host).
    
    D8: o3's live INPUT chain has DUPLICATE rules (two OCI default REJECT blocks with an unreachable copy of ESTABLISHED/icmp/lo/ntp/ssh after the first REJECT; three identical 51820 ACCEPT rules) -- residue of earlier non-idempotent manual inserts during P0 bring-up. This is o3 HOST STATE ONLY, not a repo change: a one-time dedup to one canonical rule set, folded with the D7 live-persist ops step.
    
    SCOPE SPLIT: the repo-side artifacts (persistence provisioning + doc + TestRealProvision) are this task's automated, sandbox-testable deliverable. The live-o3 dedup (D8) + apply-and-reboot-persist (D7 live-apply portion) is a MANUAL, report-only ops step (o3 is a test host) recorded in the ledger, executed by the user/operator; it does NOT gate an implement-worker.
    
    RESOLVE BOOKKEEPING (amended per review R39): merging this task's REPO-SIDE work does NOT drive D7 or D8 to `resolved` -- while o3 still holds the duplicate / non-persistent INPUT rules the defects are not actually cured. The repo merge satisfies only the repo-side portion (D7's provisioning/doc/TestRealProvision). D8 and the LIVE-APPLY portion of D7 are non-terminal on merge (manual-verification hold) and are driven to `resolved` ONLY by the RECORDED manual ops action on o3 (before/after `iptables -S INPUT` capture + post-reboot confirmation), not by the repo merge. See the acceptance for the exact operator commands and the resolution trigger.
- acceptance: |
    Repo-side (AUTOMATED gate -- the ONLY portion that resolves on merge): idempotent reboot-persistence provisioning exists and is a no-op on re-run (state-guarded); T22 install doc documents the concentrator persistence requirement; TestRealProvision asserts the persisted rule set when run against a host; `go build -tags realhosts ./...` + `go vet -tags realhosts ./...` compile. Merging this repo-side work satisfies ONLY these repo artifacts; it does NOT drive D7 or D8 to `resolved` (the live o3 host still carries the duplicate / non-persistent INPUT rules until the manual step runs).
    
    Manual ops (REPORT-ONLY, NOT an implement-worker gate, NOT resolve-on-merge -- o3 is a TEST host, NEVER deprovision it). Operator runs on o3, in order: (1) `sudo iptables -S INPUT` -- capture BEFORE; (2) dedup the INPUT chain to one canonical rule set (single `-p udp --dport 51820 -j ACCEPT`, single OCI default REJECT block; remove the duplicate/unreachable copies) -- D8; (3) apply + persist the `-i wanbond0 -j ACCEPT` rule via the new provisioning (`sudo netfilter-persistent save`, or the idempotent /etc/iptables/rules.v4 edit) -- D7 live-apply; (4) `sudo reboot`; (5) after reboot `sudo iptables -S INPUT` -- capture AFTER -- and confirm the wanbond0 ACCEPT rule SURVIVED and inbound tunnel TCP is no longer REJECTed. RESOLUTION TRIGGER: D8 and the live-apply portion of D7 are driven to `resolved` ONLY by this recorded manual ops action (both `iptables -S INPUT` captures + the post-reboot confirmation pasted into the ledger). Until that record exists, D7 stays at a manual-verification hold (open) and D8 stays open -- the repo merge alone leaves them unresolved.
- suggestedModel: standard
- ledgerRefs: ["goals:G1","defects:D7","defects:D8"]

### T49 — done

- createdAt: 2026-07-08T21:04:27.960Z
- updatedAt: 2026-07-08T21:42:03.425Z
- author: "opus-4.8[1m]"
- session: 45fdce95-2af6-42cd-8ddd-0c9faabc56ef
- headline: "D23 doc/comment sweep: measure the real 4-vCPU in-fixture ceiling, then replace the mis-attributed 150-170 Mbit/s figure in all locations (measure-then-sweep, per Q15)"
- description: |
    Group E (MEDIUM), docs/comment correctness. The netns-fixture comments state the CPU-bound tunnel throughput is '~150-170 Mbit/s on a 1-vCPU host', but that figure was measured over the REAL INTERNET with ONE daemon per host (G1 evidence: NOT CPU-bound, o3 wanbond ~24% of one core). The real IN-FIXTURE ceiling (both daemons sharing one core) on the 1-vCPU aarch64 host is 12-46 Mbit/s (docs/p0-findings.md:216-225). Every capped-fixture sizing decision derived from the 150-170 premise inherits an unsupported margin claim.
    
    Q15 answer = MEASURE-THEN-SWEEP: FIRST provision llm-ubuntu-0 (the 4-vCPU amd64 host) and run the in-fixture (both-daemons-one-core) netns throughput measurement ONCE to record the real 4-vCPU in-fixture ceiling; THEN sweep all locations replacing the mis-copied figure with the per-host MEASURED in-fixture ceilings (12-46 Mbit/s 1-vCPU aarch64 + the newly-measured 4-vCPU amd64 number) and state the rule that capped-fixture tests require 2*cap (aggregation) or cap (single-path) below the EXECUTING host's measured in-fixture ceiling.
    
    LOCATIONS to sweep: test/e2e/netns.go:21, test/e2e/fixture_impairment_test.go:11-12,63, test/e2e/fec_baseline_test.go:18, docs/p0-checkpoint.md:72.
- acceptance: "Hardware-validation (MANUAL, report-only per M10/Q12): the 4-vCPU in-fixture ceiling is measured once on llm-ubuntu-0 and recorded (before the sweep is finalized) -- this is a prerequisite step, not an automated gate. Repo-side: all listed locations (test/e2e/netns.go:21, fixture_impairment_test.go:11-12,63, fec_baseline_test.go:18, docs/p0-checkpoint.md:72) no longer state 150-170 Mbit/s as the in-fixture ceiling; each now cites the per-host MEASURED in-fixture ceilings (12-46 Mbit/s 1-vCPU + the recorded 4-vCPU number) and the '2*cap (aggregation) / cap (single-path) below the executing host's measured in-fixture ceiling' rule. `grep -rn '150-170' test/e2e docs` returns no in-fixture-ceiling misattribution. `go build -tags e2e ./...` compiles."
- suggestedModel: standard
- ledgerRefs: ["goals:G1","defects:D23"]

### T50 — done

- createdAt: 2026-07-08T21:04:34.982Z
- updatedAt: 2026-07-08T21:31:46.028Z
- author: "opus-4.8[1m]"
- session: 45fdce95-2af6-42cd-8ddd-0c9faabc56ef
- headline: "Justfile lint: cover e2e-tagged (and realhosts) sources with --build-tags (D28)"
- description: "Group E (low), tooling gap. The Justfile lint target runs `go vet ./...` + `golangci-lint run` WITHOUT `--build-tags e2e`, so test/e2e/*.go (all -tags e2e sources) is excluded from vet+lint entirely -- a T26 unused-function was invisible to `just lint` and only surfaced under `golangci-lint run --build-tags e2e ./test/e2e/`. Any future e2e-only lint defect (unused symbol, shadowing) is equally invisible to the standard gate, which is why implement-workers' green golangci-lint gate can miss e2e-tagged issues. FIX: add `--build-tags e2e` to the golangci-lint invocation and `go vet -tags e2e ./test/e2e/...` in the Justfile lint target (or a dedicated `lint-e2e` target invoked by `just lint`); fold `-tags realhosts` similarly so those sources are also covered."
- acceptance: "`just lint` now vets + lints the e2e-tagged sources: golangci-lint runs with `--build-tags e2e` and `go vet -tags e2e ./test/e2e/...` is included (realhosts sources likewise covered). Verify by introducing a deliberately-unused symbol in a `//go:build e2e` file and confirming `just lint` FLAGS it (then remove it); `just lint` passes on the clean tree. Mechanical Justfile edit only."
- suggestedModel: fast
- ledgerRefs: ["goals:G1","defects:D28"]
